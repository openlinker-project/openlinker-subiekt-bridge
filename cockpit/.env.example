VITE_API_URL=http://localhost:5005

# Shared API key for the bridge (X-Api-Key). Must match the bridge's Auth:ApiKey
# (env Auth__ApiKey). Leave empty only if the bridge runs with Auth disabled on loopback.
VITE_API_KEY=
